name: release
on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.x'
      - name: Cross-compile
        run: ./build/publish.sh ${{ github.ref_name }}
      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: bin/*
